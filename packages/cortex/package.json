{
  "name": "@archic/apollo-cortex",
  "version": "0.2.0-alpha.3",
  "private": true,
  "description": "Apollo adaptive runtime — instant, single-agent, or full plan → act → critic → verify → finalize depth over the routed provider hub and event stream.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@archic/apollo-agent": "*",
    "@archic/apollo-core": "*",
    "@archic/apollo-providers": "*",
    "@archic/apollo-router": "*"
  }
}
