{
  "name": "@archic/apollo-auth",
  "version": "0.2.0-alpha.8",
  "private": true,
  "description": "Apollo subscription auth: detect and reuse official CLI logins (Claude Code / ant, Codex, Gemini CLI) so the router can squeeze subscriptions before metered APIs.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  }
}
