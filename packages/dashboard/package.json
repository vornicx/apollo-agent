{
  "name": "@archic/apollo-dashboard",
  "version": "0.2.0-alpha.9",
  "private": true,
  "description": "Apollo local-first dashboard: a dependency-free HTTP + SSE server that renders the recorded and live event stream (runs, fleet, timelines).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@archic/apollo-core": "*",
    "@archic/apollo-router": "*"
  }
}
